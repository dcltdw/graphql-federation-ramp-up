# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Kotlin
.kotlin/

# IntelliJ IDEA
.idea/
*.iml
*.ipr
*.iws
out/

# Node (Hive CLI / gateway tooling under gateway/)
node_modules/

# Local env / credentials — note `.env*`, not `.env.*`: the narrower pattern
# missed editor backups like `.env~` in kotlin-ramp-up.
.env*
.contentfulrc.json

# Hive registry token, if the stretch goal is reached
.hive-token

# macOS
.DS_Store

# Agent worktrees
.claude/worktrees/

# Generated by dev.sh — never hand-edit
gateway/sdl/
gateway/supergraph.graphql
